node_modules
.next
out
dist
*.log
.DS_Store
.env*
!.env.example

# Raw footage stays out (10GB+ source files). The cut/encoded variants
# under public/ ARE the deployed assets and must be tracked.
assets/raw/
assets/work/
